[phases.install]
cmds = ["npm ci"]

[phases.build]
# prisma generate must run before next build (generates the Prisma client)
cmds = ["npx prisma generate", "npm run build"]

[start]
# Run pending migrations then start the server
cmd = "npx prisma migrate deploy && npm run start"
